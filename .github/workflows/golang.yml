# This workflow will build a Java project with Gradle and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-gradle

name: Golang Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build ProxyChecker on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
    steps:
      - uses: actions/checkout@v2
      - name: Set up Golang 17
        uses: actions/setup-go@v2
        with:
          go-version: '^1.17.2'
      - name: Build with Golang
        run: bash build.bash github.com/Ziloka/ProxyBroker
      - name: list files
        run: ls
      - name: Minimize Linux Executable Size (Strip debug data)
        run: strip --strip-all ProxyBroker-linux-amd64 && strip --strip-all ProxyBroker-darwin-amd64 && strip --strip-all ProxyBroker-windows-amd64.exe
      - name: Pack Executables
        run: upx -9 ProxyBroker-linux-amd64 && upx -9 strip -9 ProxyBroker-darwin-amd64 && strip -9 ProxyBroker-windows-amd64.exe
      - name: Archive code
        uses: actions/upload-artifact@v2
        with:
          name: build-artifacts
          path: './ProxyBroker*'